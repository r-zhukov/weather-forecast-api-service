version: "3.8"
services:
  weather-forecast-api-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: always
